# AGENTS.md

## Project Toolchain

This project uses the Kotlin toolchain.

Agents must use the Kotlin CLI to interact with the project. Before running project commands, inspect the available Kotlin CLI usage:

```bash
kotlin --help
```

Use the Kotlin CLI for building, testing, running, and other project interactions where applicable.
