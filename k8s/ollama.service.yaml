apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: default
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
    - port: 11434
      protocol: TCP
      targetPort: 11434