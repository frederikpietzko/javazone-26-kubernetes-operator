apiVersion: v1
kind: Service
metadata:
  name: jbproxy
  namespace: default
spec:
  type: ExternalName
  externalName: host.docker.internal
  ports:
    - port: 19516
      protocol: TCP
      targetPort: 19516