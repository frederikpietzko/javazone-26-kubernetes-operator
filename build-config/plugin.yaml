tasks:
  generateCrds:
    action: !com.example.generateCrds
      compilationArtifact: ${module.jar}
      compileClasspath: ${module.compileClasspath}
      outputDir: ${module.rootDir}/../k8s/crds

commands:
  - generateCrds
